{
  "name": "tree-sitter-blueprint",
  "version": "0.1.0",
  "description": "Tree-sitter grammar for Blueprint DSL",
  "private": true,
  "main": "bindings/node",
  "types": "bindings/node",
  "scripts": {
    "generate": "tree-sitter generate",
    "build": "tree-sitter generate && tree-sitter build --wasm",
    "test": "tree-sitter test",
    "parse": "tree-sitter parse"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "tree-sitter-cli": "^0.24.0",
    "zshy": "^0.7.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
